apiVersion: v1
kind: Secret
metadata:
  name: quantum-api-key
type: Opaque
data:
  # This remains empty in the repo. 
  # We will populate this via 'kubectl create secret' in the CI pipeline.
  IBM_TOKEN: ""